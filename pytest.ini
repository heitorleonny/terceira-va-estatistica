[pytest]
testpaths = tests
# Só coleta funções com prefixo "test_" (com underscore). Evita que funções do
# código de produção como `teste_normalidade` sejam confundidas com testes.
python_functions = test_*
